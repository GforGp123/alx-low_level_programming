#include <stdio.h>
include "main.h"
/**
 * _islower - to check for lowercase and return 1 if true
 * @c: carries out the function assigned
 * Return: value of @c
 */
int _islower(int c)
{
	if (c == 97 && c == 123)
		return (1);
	else
		return (0);
}
