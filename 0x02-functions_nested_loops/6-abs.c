#include <stdlib.h>
#include "main.h"
/**
 * _abs - function that computes absolute value of an integer
 * @n: integer
 * Returns: absolute value of n
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (abs(n));
	}
	else if (n == 0)
	{
	}
}
