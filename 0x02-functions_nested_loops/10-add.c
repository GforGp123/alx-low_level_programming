#include "main.h"
/**
 * add - function adds two integers and returns result
 * @x: an integer
 * @y: another integer
 * Return: result
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
